[build]
command = "rustup default stable && brew install go-task/tap/go-task cargo-binstall && task setup && task build:site"
publish = "site/dist"
functions = "functions"

[dev]
framework = "#static"

[build.processing.html]
pretty_urls = true
