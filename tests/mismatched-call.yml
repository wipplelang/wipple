code: |
    accepts-text :: Text -> ()
    accepts-text : ...

    -- should display 'expected `Text`, found `Number`' rather than
    -- 'cannot find instance `Add Number Number Text`'
    accepts-text (1 + 2)

output: |

diagnostics: |
    error: mismatched types
       ┌─ test:6:15
       │
     6 │ accepts-text (1 + 2)
       │               ^^^^^ expected `Text`, but found `Number`
       │
       ┌─ <dir>/std/math.wpl:15:25
       │
    15 │ + : syntax { 'a + 'b -> Add 'a 'b }
       │                         --- actual error occurred here
       │
       = for more information, see https://wipple.dev/playground/?lesson=debugging/mismatched-types
