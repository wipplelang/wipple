code: |
    get : syntax {
        get 'x -> {
          x : "Inner"
          'x
        }
    }

    x : "Outer"
    show (get x)

    let : syntax {
        let ('x : 'v) in 'y -> {
          'x : 'v
          'y
        }
    }

    let (foo : 42) in (show foo)

output: |
    Outer
    42

diagnostics: |
    warning: unused variable
      ┌─ test:9:7
      │
    3 │       x : "Inner"
      │       - actual warning occurred here
      ·
    9 │ show (get x)
      │       ^^^ `x` is never used
      │
      = for more information, see https://wipple.dev/playground/?lesson=errors/unused-variable
