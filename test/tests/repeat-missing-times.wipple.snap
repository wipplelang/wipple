Facts (layer 2):

`repeat` (VariableExpression):
  has bound(s) `Repeat-Condition` `Number` (resolved as `error instance (Repeat-Condition (n :: Number))`)
  has type `Number {()} -> ()`
  is an expression
  resolved to 1 definition(s): `repeat :: condition {body} -> () where (Repeat-Condition condition) (body :: ())`

`repeat 5 {⋯}` (ExpressionStatement):
  has type `()`

`repeat 5 {⋯}` (CallExpression):
  has type `()`
  is an expression

`5` (NumberExpression):
  has type `Number`
  is an expression
  type is annotated

`{⋯}` (BlockExpression):
  has type `{()}`
  is an expression

`show` (VariableExpression):
  has bound(s) `Describe` `String` (resolved as `instance (Describe String)`)
  has type `String -> ()`
  is an expression
  resolved to 1 definition(s): `show :: value -> () where (Describe value)`

`show "Hello, world!"` (ExpressionStatement):
  has type `()`

`show "Hello, world!"` (CallExpression):
  has type `()`
  is an expression

`"Hello, world!"` (StringExpression):
  has type `String`
  is an expression
  type is annotated



Feedback:

`repeat` (error-instance)

  Missing `times` after `5`.
  
  Try rewriting this code as `(`5` times)`, or double-check your parentheses.

  1. `repeat`: `repeat` runs `show "Hello, world!"` repeatedly.

  2. `5`: `5` is a `Number`.

  3. `show`: `show` displays `"Hello, world!"` on the screen.

  4. `"Hello, world!"`: `"Hello, world!"` is a `String`.

  (1 -> error, 2 -> 1, 4 -> 3)
