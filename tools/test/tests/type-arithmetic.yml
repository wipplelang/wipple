code: |
    Z : type
    S : N => type

    Add : A B Sum => trait
    A => instance (Add Z A A)
    A B Sum where (Add A B Sum) => instance (Add (S A) B (S Sum))


    result :: A where (Add (S (S (S Z))) (S (S Z)) A) => A
    result : ...

    _ : _ -> result

output: |

diagnostics: |
    error: could not determine the type of this expression
       ┌─ test:12:5
       │
    12 │ _ : _ -> result
       │     ^^^^^^^^^^^
       │     │
       │     try annotating the type with `::`
       │     this has type `_ -> S (S (S (S (S Z))))`
