[package]
name = "wipple-parse"
version = "0.0.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
logos = "0.16"
serde = { version = "1.0", features = ["derive"] }
typetag = "0.2"
wipple-core = { path = "../core" }
