Facts (layer 2):

`repeat` (VariableExpression):
  grouped with `condition {body} -> ()`
  has bound(s) `Repeat-Condition` `Number` (resolved as `error instance (Repeat-Condition (n :: Number))`)
  has type `Number {()} -> ()`
  is an expression
  resolved to 1 definition(s): `repeat`

`repeat 5 {⋯}` (ExpressionStatement):
  grouped with `repeat 5 {⋯}`
  has type `()`

`repeat 5 {⋯}` (CallExpression):
  grouped with `()`, `repeat 5 {⋯}`
  has type `()`
  is an expression

`5` (NumberExpression):
  grouped with `5`, `condition`
  has type `Number`
  is an expression

`{⋯}` (BlockExpression):
  grouped with `{body}`
  has type `{()}`
  is an expression

`show` (VariableExpression):
  grouped with `value -> ()`
  has bound(s) `Describe` `String` (resolved as `instance (Describe String)`)
  has type `String -> ()`
  is an expression
  resolved to 1 definition(s): `show`

`show "Hello, world!"` (ExpressionStatement):
  grouped with `show "Hello, world!"`, `body`
  has type `()`

`show "Hello, world!"` (CallExpression):
  grouped with `()`, `show "Hello, world!"`
  has type `()`
  is an expression

`"Hello, world!"` (StringExpression):
  grouped with `"Hello, world!"`, `value`
  has type `String`
  is an expression


error: 
  --> repeat-missing-times.wipple:1:1
  |
1 | repeat 5 {
  | ^^^^^^
  | |
  | Missing `times` after `5`. (Try rewriting this code as `(`5` times)`, or double-check your parentheses.)
  | `repeat` runs `show "Hello, world!"` repeatedly. This requires `Repeat-Condition` `Number`.
2 |   show "Hello, world!"
  |   ---- `show` displays `"Hello, world!"` on the screen.

