[package]
name = "wipple_bind"
version = "0.0.0"
edition = "2018"

[features]
default = ["stdlib"]
stdlib = ["wipple_stdlib"]

[dependencies]
wipple = { path = "../../../language" }
wipple_stdlib = { path = "../../../library", optional = true }
