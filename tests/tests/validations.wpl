>>> 'is' pattern

A : trait Text
A text == Text (format "A _" text)

a : A "foo"

show a
show (a as A)
show (a as (is A))
show (a into A) -- equivalent for traits

---

a ==> A foo
(a as A) ==> foo
(a as (is A)) ==> A foo
(a into A) ==> A foo
