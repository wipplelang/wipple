-- [should error]

Compatible : Left Right => trait (Left Right -> ())

Left Right where (Error ("`_` is not compatible with `_`" Left Right)) =>
    default instance (Compatible Left Right) : ...

Foo : type (Foo)
foo : Foo

Bar : type (Bar)
bar : Bar

Compatible foo bar
