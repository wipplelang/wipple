Facts (layer 2):

`(42 :: String)` (ExpressionStatement):
  grouped with `42 :: String`
  has type `Number` or `String`

`42` (NumberExpression):
  grouped with `42`, `String`
  has type `Number` or `String`
  is an expression

`String` (NamedType):
  grouped with `42`, `42 :: String`
  has type `Number` or `String`
  is a type
  resolved to 1 definition(s): `String`



Feedback:

`(42 :: String)` (conflicting-types)

  `(42 :: String)` is a `Number` or a `String`, but it can only be one of these.

  1. `String`: `42` is annotated as a `String`. This means `(42 :: String)`, `42`, and `String` must be a `Number or String`.

  (1 -> error)
