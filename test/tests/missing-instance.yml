code: |
    Foo : type

    show Foo

output: |

diagnostics: |
    error: missing instance
      --> prelude:86:17
       |
    86 | show :: A where (Show A) => A -> ()
       |                 ^^^^^^^^ could not find instance of `Show` for type `Foo -> Text`
    error: missing instance
      --> test:3:1
       |
    3  | show Foo
       | ^^^^ could not find instance of `Show` for type `Foo -> _`
