Facts (layer 2):

`(1, 2, 3) . transform ((x :: String) -> x)` (ExpressionStatement):
  assigned type `Sequence (String or Number)`
  grouped with `1, 2, 3) . transform ((x :: String) -> x)`
  has type `Sequence (String or Number)`

`1` (VisitAs):
  assigned type `String or Number`
  grouped with `_`, `1`, `element`
  has type `String` or `Number`
  is an expression

`1, 2, 3` (VisitAs):
  grouped with `container`, `3`, `container`
  has type `List (String or Number)`
  is an expression

`1, 2, 3) . transform ((x :: String) -> x)` (OperatorExpression):
  assigned type `Sequence (String or Number)`
  grouped with `transform ((x :: String) -> x)`, `(1, 2, 3) . transform ((x :: String) -> x)`
  has type `Sequence (String or Number)`
  is an expression

`2` (VisitAs):
  assigned type `String or Number`
  grouped with `_`, `2`, `element`
  has type `String` or `Number`
  is an expression

`3` (VisitAs):
  assigned type `String or Number`
  grouped with `_`, `3`, `element`
  has type `String` or `Number`
  is an expression

`transform` (VariableExpression):
  assigned type `((String or Number) -> String or Number) -> (List (String or Number)) -> Sequence (String or Number)`
  grouped with `(input -> output) -> container -> Sequence output`
  has bound(s) `As-Sequence` `(List (String or Number))` `(String or Number)` (resolved as `instance (As-Sequence (List element) element)`)
  has type `((String or Number) -> String or Number) -> (List (String or Number)) -> Sequence (String or Number)`
  is an expression
  resolved to 1 definition(s): `transform`

`transform ((x :: String) -> x)` (CallExpression):
  grouped with `Sequence output`, `Sequence output`, `1, 2, 3) . transform ((x :: String) -> x)`
  has type `Sequence (String or Number)`
  is an expression

`transform ((x :: String) -> x)` (VisitAs):
  assigned type `(List (String or Number)) -> Sequence (String or Number)`
  grouped with `container -> Sequence output`
  has type `(List (String or Number)) -> Sequence (String or Number)`
  is an expression

`(x :: String) -> x` (FunctionExpression):
  assigned type `(String or Number) -> String or Number`
  grouped with `input -> output`
  has type `(String or Number) -> String or Number`
  is an expression

`x` (VariablePattern):
  assigned type `String or Number`
  grouped with `x :: String`, `String`, `x :: String`, `x`
  has type `String` or `Number`
  is a definition
  is a pattern

`String` (NamedType):
  assigned type `String`
  grouped with `x`
  has type `String` or `Number`
  is a type
  resolved to 1 definition(s): `String`
  type is annotated

`x` (VariableExpression):
  assigned type `String or Number`
  grouped with `x`, `output`
  has type `String` or `Number`
  is an expression
  resolved to 1 definition(s): `x`
  type is annotated



Feedback:

`1` (conflicting-types)

  `1` is a `String` or a `Number`, but it can only be one of these.
  
  `1` must be the same type as `x`, `String`, `x`, and 2 others; double-check these.

  1. `transform`: `transform` transforms the `String or Number` elements in `1, 2, 3` into `String or Number` elements using `f`.<wipple-hover-link>transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)</wipple-hover-link> This requires `As-Sequence` `(List (String or Number))` `(String or Number)`.

  2. `x`: `x` is annotated with the type `String`.

  3. `3`: `3` is a `Number`.

  4. `2`: `2` is a `Number`.

  5. `1`: `1` is a `Number`.

  (2 -> 1, 3 -> 1, 4 -> 1, 5 -> error, 5 -> 1)


`2` (conflicting-types)

  `2` is a `String` or a `Number`, but it can only be one of these.
  
  `2` must be the same type as `x`, `String`, `x`, and 2 others; double-check these.

  1. `transform`: `transform` transforms the `String or Number` elements in `1, 2, 3` into `String or Number` elements using `f`.<wipple-hover-link>transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)</wipple-hover-link> This requires `As-Sequence` `(List (String or Number))` `(String or Number)`.

  2. `x`: `x` is annotated with the type `String`.

  3. `3`: `3` is a `Number`.

  4. `2`: `2` is a `Number`.

  5. `1`: `1` is a `Number`.

  (2 -> 1, 3 -> 1, 4 -> error, 4 -> 1, 5 -> 1)


`3` (conflicting-types)

  `3` is a `String` or a `Number`, but it can only be one of these.
  
  `3` must be the same type as `x`, `String`, `x`, and 2 others; double-check these.

  1. `transform`: `transform` transforms the `String or Number` elements in `1, 2, 3` into `String or Number` elements using `f`.<wipple-hover-link>transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)</wipple-hover-link> This requires `As-Sequence` `(List (String or Number))` `(String or Number)`.

  2. `x`: `x` is annotated with the type `String`.

  3. `3`: `3` is a `Number`.

  4. `2`: `2` is a `Number`.

  5. `1`: `1` is a `Number`.

  (2 -> 1, 3 -> error, 3 -> 1, 4 -> 1, 5 -> 1)


`x` (conflicting-types)

  `x` is a `String` or a `Number`, but it can only be one of these.
  
  `x` must be the same type as `String`, `x`, `1`, and 2 others; double-check these.

  1. `transform`: `transform` transforms the `String or Number` elements in `1, 2, 3` into `String or Number` elements using `f`.<wipple-hover-link>transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)</wipple-hover-link> This requires `As-Sequence` `(List (String or Number))` `(String or Number)`.

  2. `x`: `x` is annotated with the type `String`.

  3. `3`: `3` is a `Number`.

  4. `2`: `2` is a `Number`.

  5. `1`: `1` is a `Number`.

  (2 -> error, 2 -> 1, 3 -> 1, 4 -> 1, 5 -> 1)
