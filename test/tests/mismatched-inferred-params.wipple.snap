Facts (layer 2):

`(1, 2, 3) . transform ((x :: String) -> x)` (ExpressionStatement):
  has type `Sequence (Number or String)`

`1` (VisitAs):
  has type `Number` or `String`
  is an expression
  type is annotated

`1, 2, 3` (VisitAs):
  has type `List (Number or String)`
  is an expression

`1, 2, 3) . transform ((x :: String) -> x)` (OperatorExpression):
  has type `Sequence (Number or String)`
  is an expression

`2` (VisitAs):
  has type `Number` or `String`
  is an expression
  type is annotated

`3` (VisitAs):
  has type `Number` or `String`
  is an expression
  type is annotated

`transform` (VariableExpression):
  has bound(s) `As-Sequence` `(List (Number or String))` `(Number or String)` (resolved as `instance (As-Sequence (List element) element)`)
  has type `((Number or String) -> Number or String) -> (List (Number or String)) -> Sequence (Number or String)`
  is an expression
  resolved to 1 definition(s): `transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)`

`transform ((x :: String) -> x)` (CallExpression):
  has type `Sequence (Number or String)`
  is an expression

`transform ((x :: String) -> x)` (VisitAs):
  has type `(List (Number or String)) -> Sequence (Number or String)`
  is an expression

`(x :: String) -> x` (FunctionExpression):
  has type `(Number or String) -> Number or String`
  is an expression

`x` (VariablePattern):
  has type `Number` or `String`
  is a definition
  is a pattern
  type is annotated

`String` (NamedType):
  has type `Number` or `String`
  is a type
  resolved to 1 definition(s): `String : type`
  type is annotated

`x` (VariableExpression):
  has type `Number` or `String`
  is an expression
  resolved to 1 definition(s): `x`
  type is annotated



Feedback:

`x :: String` (conflicting-types)

  `x :: String` is a `Number` or a `String`, but it can only be one of these.
  
  `x :: String` must be the same type as `x`, `1`, `2`, and 7 others; double-check these.

  1. `transform`: `transform` is a `(Number -> Number) -> (List Number) -> Sequence Number`.

  2. `x`: `x` is annotated with the type `String`.

  3. `3`: `3` is a `Number`.

  4. `2`: `2` is a `Number`.

  5. `1`: `1` is a `Number`.

  (2 -> 1, 3 -> 1, 4 -> 1, 5 -> 1, 1 -> error)
