Facts (layer 2):

`(1, 2, 3) . transform ((x :: String) -> x)` (ExpressionStatement):
  has type `Sequence Number`

`1` (VisitAs):
  has type `Number` or `String`
  is an expression
  type is annotated

`1, 2, 3` (VisitAs):
  has type `List Number`
  is an expression

`1, 2, 3) . transform ((x :: String) -> x)` (OperatorExpression):
  has type `Sequence Number`
  is an expression

`2` (VisitAs):
  has type `Number` or `String`
  is an expression
  type is annotated

`3` (VisitAs):
  has type `Number` or `String`
  is an expression
  type is annotated

`transform` (VariableExpression):
  has bound(s) `As-Sequence` `(List Number)` (`Number` or `String`) (resolved as `instance (As-Sequence (List element) element)`)
  has type `(Number -> Number) -> (List Number) -> Sequence Number`
  is an expression
  resolved to 1 definition(s): `transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)`

`transform ((x :: String) -> x)` (CallExpression):
  has type `Sequence Number`
  is an expression

`transform ((x :: String) -> x)` (VisitAs):
  has type `(List Number) -> Sequence Number`
  is an expression

`(x :: String) -> x` (FunctionExpression):
  has type `Number -> Number`
  is an expression

`x` (VariablePattern):
  has type `Number` or `String`
  is a definition
  is a pattern
  type is annotated

`String` (NamedType):
  has type `Number` or `String`
  is a type
  resolved to 1 definition(s): `String : type`
  type is annotated

`x` (VariableExpression):
  has type `Number` or `String`
  is an expression
  resolved to 1 definition(s): `x`
  type is annotated



Feedback:

`transform` (conflicting-types)

  In `transform`, `input` is a `Number` or a `String`, but it can only be one of these.
  
  `input` must be the same type as `x`, `x :: String`, `1`, and 4 others; double-check these.
  
  -  `transform` is a `(input -> output) -> container -> Sequence output`.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` is annotated with the type `String`.
