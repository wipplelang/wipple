{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Coverage Report",
            "command": "rm -rf coverage && mkdir -p coverage && grcov . --binary-path target/debug/ -s . -t lcov --branch --ignore-not-existing --ignore \"/*\" -o coverage/lcov.info",
            "type": "shell"
        }
    ]
}
