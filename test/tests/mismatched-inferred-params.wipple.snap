Facts (layer 2):

`(1, 2, 3) . transform ((x :: String) -> x)` (mismatched-inferred-params.wipple:1:1-1:34) (ExpressionStatement):
  has type `Sequence Number`

`1` (mismatched-inferred-params.wipple:1:2-1:3) (VisitAs):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is an expression

`1` (mismatched-inferred-params.wipple:1:2-1:3) (ConstructorExpression):
  has bound(s) `Build-Collection` (library/src/foundation/collections.wipple:1:1-1:80) `_` `(List Number)` (resolved as `instance (Build-Collection element (List element))` (library/src/foundation/collections.wipple:12:1-12:47))
  has type `Number (List Number) -> List Number` (mismatched-inferred-params.wipple:1:2-1:3)
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)` (library/src/foundation/collections.wipple:1:1-1:80)

`1, 2, 3` (mismatched-inferred-params.wipple:1:2-1:7) (VisitAs):
  has type `List Number`
  is an expression

`1, 2, 3) . transform ((x :: String) -> x)` (mismatched-inferred-params.wipple:1:2-1:34) (OperatorExpression):
  has type `Sequence Number`
  is an expression

`2` (mismatched-inferred-params.wipple:1:4-1:5) (VisitAs):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is an expression

`2` (mismatched-inferred-params.wipple:1:4-1:5) (ConstructorExpression):
  has bound(s) `Build-Collection` (library/src/foundation/collections.wipple:1:1-1:80) `_` `(List Number)` (resolved as `instance (Build-Collection element (List element))` (library/src/foundation/collections.wipple:12:1-12:47))
  has type `Number (List Number) -> List Number` (mismatched-inferred-params.wipple:1:4-1:5)
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)` (library/src/foundation/collections.wipple:1:1-1:80)

`3` (mismatched-inferred-params.wipple:1:6-1:7) (VisitAs):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is an expression

`3` (mismatched-inferred-params.wipple:1:6-1:7) (ConstructorExpression):
  has bound(s) `Build-Collection` (library/src/foundation/collections.wipple:1:1-1:80) `_` `(List Number)` (resolved as `instance (Build-Collection element (List element))` (library/src/foundation/collections.wipple:12:1-12:47))
  has type `Number (List Number) -> List Number` (mismatched-inferred-params.wipple:1:6-1:7)
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)` (library/src/foundation/collections.wipple:1:1-1:80)

`transform` (mismatched-inferred-params.wipple:1:9-1:18) (VariableExpression):
  has bound(s) `As-Sequence` (library/src/foundation/sequence.wipple:47:1-47:71) `(List Number)` `_` (resolved as `instance (As-Sequence (List element) element)` (library/src/foundation/sequence.wipple:199:1-199:42))
  has type `(Number -> Number) -> (List Number) -> Sequence Number` (mismatched-inferred-params.wipple:1:9-1:18)
  is an expression
  resolved to 1 definition(s): `transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)` (library/src/foundation/sequence.wipple:69:1-69:86)

`transform ((x :: String) -> x)` (mismatched-inferred-params.wipple:1:9-1:34) (CallExpression):
  has type `Sequence Number`
  is an expression

`transform ((x :: String) -> x)` (mismatched-inferred-params.wipple:1:9-1:34) (VisitAs):
  has type `(List Number) -> Sequence Number` (mismatched-inferred-params.wipple:1:9-1:34)
  is an expression

`(x :: String) -> x` (mismatched-inferred-params.wipple:1:19-1:33) (FunctionExpression):
  has type `Number -> Number` (mismatched-inferred-params.wipple:1:19-1:33)
  is an expression

`x` (mismatched-inferred-params.wipple:1:20-1:21) (VariablePattern):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is a definition
  is a pattern

`x :: String` (mismatched-inferred-params.wipple:1:20-1:29) (AnnotatePattern):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is a pattern

`String` (mismatched-inferred-params.wipple:1:23-1:29) (NamedType):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is a type
  resolved to 1 definition(s): `String : type` (library/src/foundation/language.wipple:2:12-2:23)

`x` (mismatched-inferred-params.wipple:1:32-1:33) (VariableExpression):
  has type `Number` (mismatched-inferred-params.wipple:1:6-1:7) or `String` (mismatched-inferred-params.wipple:1:23-1:29)
  is an expression
  resolved to 1 definition(s): `x` (mismatched-inferred-params.wipple:1:20-1:21)



Feedback:

`1` (mismatched-inferred-params.wipple:1:2-1:3) (conflicting-types)

  `1` is a `Number` or a `String`, but it can only be one of these.
  
  `1` must be the same type as `x`, `x :: String`, `2`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`1` (mismatched-inferred-params.wipple:1:2-1:3) (conflicting-types)

  `1` is a `Number` or a `String`, but it can only be one of these.
  
  `1` must be the same type as `x`, `x :: String`, `2`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`2` (mismatched-inferred-params.wipple:1:4-1:5) (conflicting-types)

  `2` is a `Number` or a `String`, but it can only be one of these.
  
  `2` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`2` (mismatched-inferred-params.wipple:1:4-1:5) (conflicting-types)

  `2` is a `Number` or a `String`, but it can only be one of these.
  
  `2` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`3` (mismatched-inferred-params.wipple:1:6-1:7) (conflicting-types)

  `3` is a `Number` or a `String`, but it can only be one of these.
  
  `3` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`3` (mismatched-inferred-params.wipple:1:6-1:7) (conflicting-types)

  `3` is a `Number` or a `String`, but it can only be one of these.
  
  `3` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`x` (mismatched-inferred-params.wipple:1:20-1:21) (conflicting-types)

  `x` is a `Number` or a `String`, but it can only be one of these.
  
  `x` must be the same type as `x :: String`, `1`, `2`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`x :: String` (mismatched-inferred-params.wipple:1:20-1:29) (conflicting-types)

  `x :: String` is a `Number` or a `String`, but it can only be one of these.
  
  `x :: String` must be the same type as `x`, `1`, `2`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`String` (mismatched-inferred-params.wipple:1:23-1:29) (conflicting-types)

  `String` is a `Number` or a `String`, but it can only be one of these.
  
  `String` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.


`x` (mismatched-inferred-params.wipple:1:32-1:33) (conflicting-types)

  `x` is a `Number` or a `String`, but it can only be one of these.
  
  `x` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `3` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
  
  -  `input` must have the same type everywhere in this use of `transform`.
  
  -  `output` must have the same type everywhere in this use of `transform`.
