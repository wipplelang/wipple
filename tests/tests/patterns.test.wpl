>>> 'is' pattern

A : trait Text
A text == Text (format "A _" text)

a : A "foo"

debug a
debug (a as A)
debug (a as (is A))
debug (a into A) -- equivalent for traits

---

a ==> A foo
(a as A) ==> foo
(a as (is A)) ==> A foo
(a into A) ==> A foo
