Facts (layer 2):

`(42 :: String)` (ExpressionStatement):
  has type `Number` or `String`

`42` (NumberExpression):
  has type `Number` or `String`
  is an expression
  type is annotated

`String` (NamedType):
  has type `Number` or `String`
  is a type
  resolved to 1 definition(s): `String : type`
  type is annotated



Feedback:

`(42 :: String)` (conflicting-types)

  `(42 :: String)` is a `Number` or a `String`, but it can only be one of these.
  
  `(42 :: String)` must be the same type as `42`, `42 :: String`, and `String`; double-check these.
  
  -  `42` is a `Number`.
