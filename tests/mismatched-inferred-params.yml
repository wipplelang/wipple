code: |
    list 1 2 3 . transform ((_ :: Text) -> ())

output: |

diagnostics: |
    error: mismatched types
      ┌─ test:1:24
      │
    1 │ list 1 2 3 . transform ((_ :: Text) -> ())
      │                        ^^^^^^^^^^^^^^^^^^^
      │                        │
      │                        expected `Number -> ()`, but found `Text -> ()`
      │                        this function accepts `Text`, but it should accept `Number`
      │
      = for more information, see https://wipple.dev/playground/?lesson=debugging/mismatched-types
