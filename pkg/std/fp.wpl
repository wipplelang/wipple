[[no-std]]

[help "Returns its input unmodified."]
it :: A => A -> A
it : x -> x

[help "Ignores its second input."]
just :: A => A -> _ -> A
just : x -> _ -> x

[operator Composition-Precedence]
. : syntax {
	'x . 'f -> 'f 'x
}
