>>> 'return' from functions and computed variables

f : _ -> {
    show "f: Before return"
    return 42
    show "f: After return"
}

x :> {
    show "x: Before return"
    return 42
    show "x: After return"
}

show (f _)
show x

---

f: Before return ==> f: Before return
(f _) ==> 42
x: Before return ==> x: Before return
x ==> 42

>>> Loops

i : 1

x : loop {
    i : i * 2
    when (i > 100) (break i)
}

show x

---

x ==> 128
