Facts (layer 2):

`show` (VariableExpression):
  assigned type `(String or Number) -> ()`
  grouped with `value -> ()`
  has bound(s) `Describe` `(String or Number)` (resolved as `instance (Describe String)`)
  has type `(String or Number) -> ()`
  is an expression
  resolved to 1 definition(s): `show`

`show (when True {⋯})` (ExpressionStatement):
  assigned type `()`
  grouped with `show (when True {⋯})`
  has type `()`

`show (when True {⋯})` (CallExpression):
  grouped with `()`, `show (when True {⋯})`
  has type `()`
  is an expression

`when True {⋯}` (WhenExpression):
  assigned type `String`
  grouped with `42`, `"hi"`, `value`
  has type `String` or `Number`
  is an expression

`True` (ConstructorExpression):
  assigned type `Boolean`
  grouped with `Boolean`, `True`, `False`
  has type `Boolean`
  is an expression
  resolved to 1 definition(s): `True`

`True` (ConstructorPattern):
  assigned type `Boolean`
  grouped with `True`, `Boolean`
  has type `Boolean`
  is a pattern
  resolved to 1 definition(s): `True`

`42` (NumberExpression):
  assigned type `Number`
  grouped with `42`, `when True {⋯}`
  has type `String` or `Number`
  is an expression

`False` (ConstructorPattern):
  assigned type `Boolean`
  grouped with `True`, `Boolean`
  has type `Boolean`
  is a pattern
  resolved to 1 definition(s): `False`

`"hi"` (StringExpression):
  assigned type `String`
  grouped with `"hi"`, `when True {⋯}`
  has type `String` or `Number`
  is an expression



Feedback:

`when True {⋯}` (conflicting-types)

  `when True {⋯}` is a `String` or a `Number`, but it can only be one of these.
  
  `when True {⋯}` must be the same type as `42` and `"hi"`; double-check these.

  1. `show`: `show` displays `when True {⋯}` on the screen.

  2. `42`: `42` is a `Number`.

  3. `"hi"`: `"hi"` is a `String`.

  (2 -> 1, 3 -> 1, 1 -> error)


`42` (conflicting-types)

  `42` is a `String` or a `Number`, but it can only be one of these.
  
  `42` must be the same type as `when True {⋯}` and `"hi"`; double-check these.

  1. `show`: `show` displays `when True {⋯}` on the screen.

  2. `42`: `42` is a `Number`.

  3. `"hi"`: `"hi"` is a `String`.

  (2 -> error, 2 -> 1, 3 -> 1)


`"hi"` (conflicting-types)

  `"hi"` is a `String` or a `Number`, but it can only be one of these.
  
  `"hi"` must be the same type as `when True {⋯}` and `42`; double-check these.

  1. `show`: `show` displays `when True {⋯}` on the screen.

  2. `42`: `42` is a `Number`.

  3. `"hi"`: `"hi"` is a `String`.

  (2 -> 1, 3 -> error, 3 -> 1)
