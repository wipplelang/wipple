Facts (layer 2):

`show` (VariableExpression):
  has bound(s) `Describe` (`Number` or `String`) (unresolved)
  has type `Number -> ()`
  is an expression
  resolved to 1 definition(s): `show :: value -> () where (Describe value)`

`show (when True {⋯})` (ExpressionStatement):
  has type `()`

`show (when True {⋯})` (CallExpression):
  has type `()`
  is an expression

`when True {⋯}` (WhenExpression):
  has type `Number` or `String`
  is an expression

`True` (ConstructorExpression):
  has type `Boolean`
  is an expression
  resolved to 1 definition(s): `True`

`True` (ConstructorPattern):
  has type `Boolean`
  is a pattern
  resolved to 1 definition(s): `True`

`42` (NumberExpression):
  has type `Number` or `String`
  is an expression

`False` (ConstructorPattern):
  has type `Boolean`
  is a pattern
  resolved to 1 definition(s): `False`

`"hi"` (StringExpression):
  has type `Number` or `String`
  is an expression



Feedback:

`when True {⋯}` (conflicting-types)

  `when True {⋯}` is a `Number` or a `String`, but it can only be one of these.
  
  `when True {⋯}` must be the same type as `"hi"` and `42`; double-check these.
  
  -  Display a value on the screen.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.


`42` (conflicting-types)

  `42` is a `Number` or a `String`, but it can only be one of these.
  
  `42` must be the same type as `"hi"` and `when True {⋯}`; double-check these.
  
  -  Display a value on the screen.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.


`"hi"` (conflicting-types)

  `"hi"` is a `Number` or a `String`, but it can only be one of these.
  
  `"hi"` must be the same type as `42` and `when True {⋯}`; double-check these.
  
  -  Display a value on the screen.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.


`42` (conflicting-types)

  `42` is a `Number` or a `String`, but it can only be one of these.
  
  `42` must be the same type as `"hi"` and `when True {⋯}`; double-check these.
  
  -  Display a value on the screen.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.


`"hi"` (conflicting-types)

  `"hi"` is a `Number` or a `String`, but it can only be one of these.
  
  `"hi"` must be the same type as `42` and `when True {⋯}`; double-check these.
  
  -  Display a value on the screen.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.
