code: |
    show 1 + 2
    show True or False
    show (1 + "hi")

output: |

diagnostics: |
    error: cannot add `()` to `Number`
       ┌─ test:1:8
       │
     1 │ show 1 + 2
       │        ^
       │        │
       │        could not find instance `Add () Number _`
       │        operators consume all expressions on either side; you may be missing parentheses
       │
       ┌─ <dir>/std/math.wpl:15:25
       │
    15 │ + : syntax { 'a + 'b -> Add 'a 'b }
       │                         --- actual error occurred here

    error: missing instance
       ┌─ test:2:11
       │
     2 │ show True or False
       │           ^^
       │           │
       │           could not find instance `Or () Boolean _`
       │           operators consume all expressions on either side; you may be missing parentheses
       │
       ┌─ <dir>/std/logic.wpl:41:27
       │
    41 │ or : syntax { 'a or 'b -> Or 'a (() -> 'b) }
       │                           -- actual error occurred here

    error: cannot add `Number` to `Text`
       ┌─ test:3:9
       │
     3 │ show (1 + "hi")
       │         ^ could not find instance `Add Number Text _`
       │
       ┌─ <dir>/std/math.wpl:15:25
       │
    15 │ + : syntax { 'a + 'b -> Add 'a 'b }
       │                         --- actual error occurred here
