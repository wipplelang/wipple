code: |
    produces-text : () -> "Hello, world!"
    displays-text : (text :: Text) -> show text

    displays-text produces-text

output: |

diagnostics: |
    error: mismatched types
      ┌─ test:4:15
      │
    4 │ displays-text produces-text
      │               ^^^^^^^^^^^^^
      │               │
      │               expected `Text`, but found `() -> Text`
      │               try providing an input to this function
