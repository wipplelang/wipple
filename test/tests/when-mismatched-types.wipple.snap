Facts (layer 2):

`show` (when-mismatched-types.wipple:1:1-1:5) (VariableExpression):
  has bound(s) `Describe` (library/src/foundation/io.wipple:2:1-2:44) `_` (unresolved)
  has type `Number -> ()` (when-mismatched-types.wipple:1:1-1:5)
  is an expression
  resolved to 1 definition(s): `show :: value -> () where (Describe value)` (library/src/foundation/io.wipple:11:1-11:43)

`show (when True {⋯})` (when-mismatched-types.wipple:1:1-4:3) (ExpressionStatement):
  has type `()`

`show (when True {⋯})` (when-mismatched-types.wipple:1:1-4:3) (CallExpression):
  has type `()`
  is an expression

`when True {⋯}` (when-mismatched-types.wipple:1:7-4:2) (WhenExpression):
  has type `Number` (when-mismatched-types.wipple:2:11-2:13) or `String` (when-mismatched-types.wipple:3:12-3:16)
  is an expression

`True` (when-mismatched-types.wipple:1:12-1:16) (ConstructorExpression):
  has type `Boolean`
  is an expression
  resolved to 1 definition(s): `True` (library/src/foundation/logic.wipple:4:3-4:7)

`True` (when-mismatched-types.wipple:2:3-2:7) (ConstructorPattern):
  has type `Boolean`
  is a pattern
  resolved to 1 definition(s): `True` (library/src/foundation/logic.wipple:4:3-4:7)

`42` (when-mismatched-types.wipple:2:11-2:13) (NumberExpression):
  has type `Number` (when-mismatched-types.wipple:2:11-2:13) or `String` (when-mismatched-types.wipple:3:12-3:16)
  is an expression

`False` (when-mismatched-types.wipple:3:3-3:8) (ConstructorPattern):
  has type `Boolean`
  is a pattern
  resolved to 1 definition(s): `False` (library/src/foundation/logic.wipple:3:3-3:8)

`"hi"` (when-mismatched-types.wipple:3:12-3:16) (StringExpression):
  has type `Number` (when-mismatched-types.wipple:2:11-2:13) or `String` (when-mismatched-types.wipple:3:12-3:16)
  is an expression



Feedback:

`when True {⋯}` (when-mismatched-types.wipple:1:7-4:2) (conflicting-types)

  `when True {⋯}` is a `Number` or a `String`, but it can only be one of these.
  
  `when True {⋯}` must be the same type as `"hi"` and `42`; double-check these.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.
  
  -  `value` must have the same type everywhere in this use of `show`.


`42` (when-mismatched-types.wipple:2:11-2:13) (conflicting-types)

  `42` is a `Number` or a `String`, but it can only be one of these.
  
  `42` must be the same type as `"hi"` and `when True {⋯}`; double-check these.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.
  
  -  `value` must have the same type everywhere in this use of `show`.


`42` (when-mismatched-types.wipple:2:11-2:13) (conflicting-types)

  `42` is a `Number` or a `String`, but it can only be one of these.
  
  `42` must be the same type as `"hi"` and `when True {⋯}`; double-check these.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.
  
  -  `value` must have the same type everywhere in this use of `show`.


`"hi"` (when-mismatched-types.wipple:3:12-3:16) (conflicting-types)

  `"hi"` is a `Number` or a `String`, but it can only be one of these.
  
  `"hi"` must be the same type as `42` and `when True {⋯}`; double-check these.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.
  
  -  `value` must have the same type everywhere in this use of `show`.


`"hi"` (when-mismatched-types.wipple:3:12-3:16) (conflicting-types)

  `"hi"` is a `Number` or a `String`, but it can only be one of these.
  
  `"hi"` must be the same type as `42` and `when True {⋯}`; double-check these.
  
  -  `42` is a `Number`.
  
  -  `"hi"` is a `String`.
  
  -  `value` must have the same type everywhere in this use of `show`.
