# Bytecode

Wipple bytecode is the format for compiled Wipple files. Bytecode comes in two flavors: modules, which use relative references, and objects, which use absolute references and can be executed by a backend. Modules can be linked into a single object.
