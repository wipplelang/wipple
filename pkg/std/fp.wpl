[[no-std]]

[help "Returns its input unmodified."]
it :: A => A -> A
it : x -> x

[help "Ignores its second input."]
just :: A => A -> _ -> A
just : x -> _ -> x

. : dot operator (x f ~> f x)

Map : A B Input Output => trait ((A -> B) -> Input -> Output)
| : dot operator (x f ~> Map f x)
