Facts (layer 2):

`show` (ExpressionStatement):
  assigned type `_ -> ()`
  grouped with `show`
  has type `_ -> ()`

`show` (VariableExpression):
  assigned type `_ -> ()`
  grouped with `value -> ()`, `show`
  has type `_ -> ()`
  is an expression
  resolved to 1 definition(s): `show`



Feedback:

`show` (incomplete-type)

  Missing information for the type of `show`.
  
  Wipple determined this code is a `_ -> ()`, but it needs some more information for the `_` placeholders.


`show` (incomplete-type)

  Missing information for the type of `show`.
  
  Wipple determined this code is a `_ -> ()`, but it needs some more information for the `_` placeholders.
