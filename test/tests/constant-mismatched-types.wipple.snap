Facts (layer 2):

`x` (ConstantDefinition):
  assigned type `Number`
  grouped with `Number`, `"42"`
  has definition constraints
  has type `String` or `Number`
  is a definition

`Number` (NamedType):
  assigned type `Number`
  grouped with `x`
  has type `String` or `Number`
  is a type
  resolved to 1 definition(s): `Number`
  type is annotated

`x : "42"` (AssignmentStatement):
  assigned type `()`
  has type `()`

`"42"` (StringExpression):
  assigned type `String`
  grouped with `"42"`, `x`
  has type `String` or `Number`
  is an expression



Feedback:

`x` (conflicting-types)

  `x` is a `String` or a `Number`, but it can only be one of these.
  
  `x` must be the same type as `Number` and `"42"`; double-check these.


`"42"` (conflicting-types)

  `"42"` is a `String` or a `Number`, but it can only be one of these.
  
  `"42"` must be the same type as `x` and `Number`; double-check these.
