Facts (layer 2):

`accepts-text :: String -> ()` (ConstantDefinition):
  has definition constraints
  has type `String -> ()`
  is a definition

`String` (NamedType):
  has type `String`
  is a type
  resolved to 1 definition(s): `String : type`

`String -> ()` (FunctionType):
  has type `String -> ()`
  is a type

`()` (UnitType):
  has type `()`
  is a type

`accepts-text : todo` (AssignmentStatement):
  has type `()`

`todo` (VariableExpression):
  has type `String -> ()`
  is an expression
  resolved to 1 definition(s): `todo :: value`

`-- should display 'expected `String`, found `Number`' rather than
-- 'cannot find instance `Add Number Number String`'
accepts-text (1 + 2)` (ExpressionStatement):
  has type `()`

`accepts-text` (VariableExpression):
  has type `Number -> ()`
  is an expression
  resolved to 1 definition(s): `accepts-text :: String -> ()`

`accepts-text (1 + 2)` (CallExpression):
  has type `()`
  is an expression

`1` (NumberExpression):
  has type `Number`
  is an expression

`1 + 2` (OperatorExpression):
  has type `Number` or `String`
  is an expression

`1 + 2` (CallExpression):
  has type `Number` or `String`
  is an expression

`1 + 2` (ConstructorExpression):
  has bound(s) `Add` `Number` `Number` `_` (resolved as `instance (Add Number Number Number)`)
  has type `Number Number -> Number`
  is an expression
  resolved to 1 definition(s): `Add : left right (infer sum) => trait (left right -> sum)`

`2` (NumberExpression):
  has type `Number`
  is an expression



Feedback:

`1 + 2` (conflicting-types)

  `1 + 2` is a `Number` or a `String`, but it can only be one of these.
  
  `1 + 2` must be the same type as ; double-check these.
  
  -  `(infer sum)` must have the same type everywhere in this use of `_`.


`1 + 2` (conflicting-types)

  `1 + 2` is a `Number` or a `String`, but it can only be one of these.
  
  `1 + 2` must be the same type as ; double-check these.
  
  -  `(infer sum)` must have the same type everywhere in this use of `_`.
