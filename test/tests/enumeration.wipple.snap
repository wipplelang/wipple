Facts (layer 2):

`Grade` (TypeDefinition):
  grouped with `A`, `B`, `C`, `D`, `F`
  has definition constraints
  has type `Grade`
  is a definition

`A` (VariantDefinition):
  grouped with `Grade`
  has definition constraints
  has type `Grade`
  is a definition

`B` (VariantDefinition):
  grouped with `Grade`
  has definition constraints
  has type `Grade`
  is a definition

`C` (VariantDefinition):
  grouped with `Grade`
  has definition constraints
  has type `Grade`
  is a definition

`D` (VariantDefinition):
  grouped with `Grade`
  has definition constraints
  has type `Grade`
  is a definition

`F` (VariantDefinition):
  grouped with `Grade`
  has definition constraints
  has type `Grade`
  is a definition

`_` (WildcardPattern):
  grouped with `A`, `A`
  has type `Grade`
  is a pattern

`_ : A` (AssignmentStatement):
  has type `()`

`A` (ConstructorExpression):
  grouped with `Grade`, `_`, `_`
  has type `Grade`
  is an expression
  resolved to 1 definition(s): `A`


Output:
