[package]
name = "wipple-syntax"
version = "0.0.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
itertools = "0.14"
serde = { version = "1.0", features = ["derive"] }
typetag = "0.2"
wipple-core = { path = "../core" }
wipple-parse = { path = "../parse" }
