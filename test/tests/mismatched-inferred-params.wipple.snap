Facts (layer 2):

`(1, 2, 3) . transform ((x :: String) -> x)` (ExpressionStatement):
  has type `Sequence Number`

`1` (VisitAs):
  has type `Number` or `String`
  is an expression

`1` (ConstructorExpression):
  has bound(s) `Build-Collection` (`Number` or `String`) `(List Number)` (resolved as `instance (Build-Collection element (List element))`)
  has type `Number (List Number) -> List Number`
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)`

`1, 2, 3` (VisitAs):
  has type `List Number`
  is an expression

`1, 2, 3) . transform ((x :: String) -> x)` (OperatorExpression):
  has type `Sequence Number`
  is an expression

`2` (VisitAs):
  has type `Number` or `String`
  is an expression

`2` (ConstructorExpression):
  has bound(s) `Build-Collection` (`Number` or `String`) `(List Number)` (resolved as `instance (Build-Collection element (List element))`)
  has type `Number (List Number) -> List Number`
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)`

`3` (VisitAs):
  has type `Number` or `String`
  is an expression

`3` (ConstructorExpression):
  has bound(s) `Build-Collection` (`Number` or `String`) `(List Number)` (resolved as `instance (Build-Collection element (List element))`)
  has type `Number (List Number) -> List Number`
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)`

`transform` (VariableExpression):
  has bound(s) `As-Sequence` `(List Number)` (`Number` or `String`) (resolved as `instance (As-Sequence (List element) element)`)
  has type `(Number -> Number) -> (List Number) -> Sequence Number`
  is an expression
  resolved to 1 definition(s): `transform :: (input -> output) -> container -> Sequence output where (As-Sequence container input)`

`transform ((x :: String) -> x)` (CallExpression):
  has type `Sequence Number`
  is an expression

`transform ((x :: String) -> x)` (VisitAs):
  has type `(List Number) -> Sequence Number`
  is an expression

`(x :: String) -> x` (FunctionExpression):
  has type `Number -> Number`
  is an expression

`x` (VariablePattern):
  has type `Number` or `String`
  is a definition
  is a pattern

`String` (NamedType):
  has type `Number` or `String`
  is a type
  resolved to 1 definition(s): `String : type`

`x` (VariableExpression):
  has type `Number` or `String`
  is an expression
  resolved to 1 definition(s): `x`



Feedback:

`x` (conflicting-types)

  `x` is a `Number` or a `String`, but it can only be one of these.
  
  `x` must be the same type as `x :: String`, `1`, `2`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`x :: String` (conflicting-types)

  `x :: String` is a `Number` or a `String`, but it can only be one of these.
  
  `x :: String` must be the same type as `x`, `1`, `2`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`1` (conflicting-types)

  `1` is a `Number` or a `String`, but it can only be one of these.
  
  `1` must be the same type as `x`, `x :: String`, `2`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`2` (conflicting-types)

  `2` is a `Number` or a `String`, but it can only be one of these.
  
  `2` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`3` (conflicting-types)

  `3` is a `Number` or a `String`, but it can only be one of these.
  
  `3` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`x` (conflicting-types)

  `x` is a `Number` or a `String`, but it can only be one of these.
  
  `x` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`1` (conflicting-types)

  `1` is a `Number` or a `String`, but it can only be one of these.
  
  `1` must be the same type as `x`, `x :: String`, `2`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`2` (conflicting-types)

  `2` is a `Number` or a `String`, but it can only be one of these.
  
  `2` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`3` (conflicting-types)

  `3` is a `Number` or a `String`, but it can only be one of these.
  
  `3` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.


`String` (conflicting-types)

  `String` is a `Number` or a `String`, but it can only be one of these.
  
  `String` must be the same type as `x`, `x :: String`, `1`, and 3 others; double-check these.
  
  -  `1` is a `Number`.
  
  -  `2` is a `Number`.
  
  -  `3` is a `Number`.
  
  -  `x` was annotated with the type `String`.
  
  -  `1` returns a `List Number`.
  
  -  `2` returns a `List Number`.
  
  -  `3` returns a `List Number`.
  
  -  `1` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `1, 2, 3` requires the instance `Initial-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `2` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
  
  -  `3` requires the instance `Build-Collection` (`Number` or `String`) `(List Number)`.
