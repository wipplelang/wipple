Facts (layer 2):

`my-list` (VariablePattern):
  has type `List ()`
  is a definition
  is a pattern

`my-list : 1, "hi", ()` (AssignmentStatement):
  has type `()`

`1` (VisitAs):
  has type `()` or `String` or `Number`
  is an expression

`1` (ConstructorExpression):
  has bound(s) `Build-Collection` `_` `(List ())` (resolved as `default error instance (Build-Collection element collection)`)
  has type `() (List ()) -> List ()`
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)`

`1, "hi", ()` (CollectionExpression):
  has type `List ()`
  is an expression

`"hi"` (VisitAs):
  has type `()` or `String` or `Number`
  is an expression

`"hi"` (ConstructorExpression):
  has bound(s) `Build-Collection` `_` `(List ())` (resolved as `default error instance (Build-Collection element collection)`)
  has type `() (List ()) -> List ()`
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)`

`()` (VisitAs):
  has type `()` or `String` or `Number`
  is an expression

`()` (ConstructorExpression):
  has bound(s) `Build-Collection` `_` `(List ())` (resolved as `instance (Build-Collection element (List element))`)
  has type `() (List ()) -> List ()`
  is an expression
  resolved to 1 definition(s): `Build-Collection : (infer element) collection => trait (element collection -> collection)`

`my-list` (VisitAs):
  has type `List ()`
  is an expression
  resolved to 1 definition(s): `my-list`

`my-list . each show` (ExpressionStatement):
  has type `()`

`my-list . each show` (OperatorExpression):
  has type `()`
  is an expression

`each` (VariableExpression):
  has bound(s) `As-Sequence` `(List ())` `_` (resolved as `default error instance (As-Sequence container element)`)
  has type `(_ -> ()) -> (List ()) -> ()`
  is an expression
  resolved to 1 definition(s): `each :: (element -> ()) -> container -> () where (As-Sequence container element)`

`each show` (CallExpression):
  has type `()`
  is an expression

`each show` (VisitAs):
  has type `(List ()) -> ()`
  is an expression

`show` (VariableExpression):
  has type `_ -> ()`
  is an expression
  resolved to 1 definition(s): `show :: value -> () where (Describe value)`



Feedback:

`1` (conflicting-types)

  `1` is a `()`, `String`, or a `Number`, but it can only be one of these.
  
  `1` must be the same type as `"hi"` and `()`; double-check these.
  
  -  `"hi"` is a `String`.
  
  -  `1` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.


`1` (conflicting-types)

  `1` is a `()`, `String`, or a `Number`, but it can only be one of these.
  
  `1` must be the same type as `"hi"` and `()`; double-check these.
  
  -  `"hi"` is a `String`.
  
  -  `1` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.


`"hi"` (conflicting-types)

  `"hi"` is a `()`, `String`, or a `Number`, but it can only be one of these.
  
  `"hi"` must be the same type as `1` and `()`; double-check these.
  
  -  `"hi"` is a `String`.
  
  -  `1` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.


`"hi"` (conflicting-types)

  `"hi"` is a `()`, `String`, or a `Number`, but it can only be one of these.
  
  `"hi"` must be the same type as `1` and `()`; double-check these.
  
  -  `"hi"` is a `String`.
  
  -  `1` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.


`()` (conflicting-types)

  `()` is a `()`, `String`, or a `Number`, but it can only be one of these.
  
  `()` must be the same type as `1` and `"hi"`; double-check these.
  
  -  `"hi"` is a `String`.
  
  -  `1` is a `Number`.
  
  -  `element` must have the same type everywhere in this use of `_`.
