code: |
    T : A B => trait (A , B)

    foo :: A where (T A _) => A -> ()
    foo : just ()

    A : type

    B : type
    instance (T B A) : ...

    C : type
    X where (T _ A) => instance (T X A) : ...

output: |

diagnostics: |
